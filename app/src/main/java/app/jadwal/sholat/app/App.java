/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package app.jadwal.sholat.app;

import app.jadwal.sholat.list.LinkedList;

import static app.jadwal.sholat.utilities.StringUtils.join;
import static app.jadwal.sholat.utilities.StringUtils.split;
import static app.jadwal.sholat.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
